{
  "name": "attendance-salary-app",
  "version": "1.0.0",
  "description": "Attendance & Salary Management System",
  "main": "index.html",
  "scripts": {
    "start": "npx serve .",
    "build": "tar -czvf attendance-salary-app-1.0.0.tgz index.html style.css app.js",
    "package": "npm run build",
    "test": "node -e \"const assert = require('assert'); const calcInr = (b, d, p) => { const daily = b/d; const eBasic = daily * p; const hra = eBasic * 0.20; const gross = eBasic + hra; const pf = eBasic * 0.12; const pt = gross > 15000 ? 200 : 0; const tds = gross > 50000 ? gross * 0.10 : 0; return parseFloat((gross - (pf + pt + tds)).toFixed(2)); }; assert.strictEqual(calcInr(95000, 22, 22), 91000); console.log('✔ All Indian Payroll (INR) Unit Tests Passed!'); process.exit(0);\""
  },
  "keywords": ["attendance", "payroll", "salary", "management"],
  "author": "DevOps Team",
  "license": "MIT"
}
